// Con parámetros
function contarFor(pnum){
    let numero = pnum; 

    for(let contar=1;contar <=numero; contar++){
        console.log(contar);
    }
}
// como expresión
const contarForExp = function (pnum){
    let numero = pnum; 

    for(let contar=1;contar <=numero; contar++){
        console.log(contar);
    }
}