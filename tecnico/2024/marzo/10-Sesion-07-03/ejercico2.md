* Leer un número entero e imprimir si es par o impar y si es positivo o negativo.
```
INICIO
    DEFINIR n COMO REAL

    n=0

    ESCRIBIR "Ingrese un número"

    SI (!EsNumero(n)) ENTONCES
        SI n%2==0 y  ENTONCES

FIN
``` 